Kubernetes Dashboard Setup
This is a  Demonstration on setting up the Kubernetes Dashboard which is a graphical web interface to manage the cluster

kubectl apply -f https://raw.githubusercontent.com/kubernetes/dashboard/v2.7.0/aio/deploy/recommended.yaml

kubectl apply -f - <<EOF
apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-user
  namespace: kubernetes-dashboard
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: admin-user
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: admin-user
  namespace: kubernetes-dashboard
EOF


kubectl -n kubernetes-dashboard port-forward svc/kubernetes-dashboard 8443:443
# in another terminal:
kubectl -n kubernetes-dashboard create token admin-user

Accessing the Dashboard –The dashboard can be accessed from your local browser
 from your laptop terminal run this command:
ssh -i <your-key>.pem -L 8443:localhost:8443 ec2-user@<CONTROL_PLANE_PUBLIC_IP>

Then, while the tunnel stays open, open in your browser:

https://localhost:8443/


